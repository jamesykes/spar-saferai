# Data

- `raw/` contains the source elicitation CSV.
- `processed/` contains reproducible cleaned outputs generated by `scripts/01_clean_data.py`.

The raw CSV is treated as an input artifact. The processed CSVs can be regenerated from the scripts.

